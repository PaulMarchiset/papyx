/**
 * Kept in step with package.json, src-tauri/Cargo.toml and
 * src-tauri/tauri.conf.json by hand — the four of them are the only places the
 * version appears.
 */
export const APP_VERSION = "1.0.0";
